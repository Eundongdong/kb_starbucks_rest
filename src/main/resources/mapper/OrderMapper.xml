<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.order.mapper.OrderMapper">
    <insert id="insert" parameterType="OrderInfo">
        INSERT INTO order_info(order_id)
        VALUES (#{orderId});
        <foreach collection="OrderLine" item="line" separator=",">
            INSERT INTO order_line (order_no,order_prod_no, order_quantity)
            VALUES (LAST_INSERT_ID(),#{line.orderP().getProdNo()},#{line.orderQuantity())";
        </foreach>
    </insert>


    <select id="findByOrderId" resultType="OrderInfo">
        SELECT info.order_no, order_id, order_dt,
        order_prod_no, prod_name, prod_price, order_quantity
         FROM order_info info JOIN order_line line ON info.order_no = line.order_no
         JOIN product p ON line.order_prod_no = p.prod_no
         WHERE info.order_id = #{loginedId}
         ORDER BY order_no DESC
    </select>
</mapper>